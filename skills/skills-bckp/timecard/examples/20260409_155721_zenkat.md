# Session Worklog — 2026-04-09

**Started:** 2026-04-09 13:10:00
**Ended:** 2026-04-09 15:57:21
**Duration:** 2h 47m

## Summary

Continued Phase 2 Supabase backend integration. Main focus this session: diagnosing and fixing a run-configuration issue (wrong API key, mock mode still on) that was blocking real staging tests, and wiring the Profile page to read from the database.

### What was done

**Profile page reads from DB**
Profile.tsx was reading entirely from localStorage — so signed-in staging users always saw the mock user (Janine, 48) instead of their own data. Added a `useEffect` that calls `getCurrentUser()` on mount and merges the result into component state, following the same pattern already applied to Home.tsx. Also wired `signOut()` into the logout handler so the Supabase session is properly cleared on log out.

**Fixed staging configuration**
Two env issues were blocking real DB access:
- `VITE_USE_MOCK=true` was still set — all API calls were going to mock data regardless of auth state. Set to `false`.
- `VITE_SUPABASE_ANON_KEY` was set to the service role (secret) key, which Supabase blocks from browser use. Updated to the publishable key.
- Added `VITE_SKIP_OTP=true` since the email confirmation toggle remains locked in the Supabase dashboard.

**TODO updated**
Added a "Staging / DB gaps" section documenting the icebreaker issue: `rowToUser()` returns `icebreakers: []`, which leaves the "Send to [name]" button on MatchDetail permanently disabled in staging mode.

### Phase 2 status

Done: auth (email + phone OTP), protected routes, profile creation on sign-up, seed script (500 users), generated TypeScript types, RLS, sign-in flow, matches/outings/conversations reading from DB, chat write path + Realtime, profile reading from DB, staging environment working.

Remaining: profile edits write to DB, onboarding fields persist to DB, photo upload (Supabase Storage), match interests write to `match_interests` table, outing join/leave write to `outing_members`, viewed/saved matches persist to DB, notifications, reports/blocking, icebreaker generation.

## Commits

- `65fa9b5` fix: show logged-in user's first name in Home greeting
- `cb00346` fix: wire Log In button on Splash to /signin instead of /home
- `d948a48` feat: implement chat write path with Supabase Realtime
- `5a4894a` fix: set tab title to BeFriend and replace Vite favicon with pink heart
- `91b8c18` feat: add sign-in flow

## Open items going into next session

- Profile edits (saveEdit) need to write to the `profiles` table in Supabase
- Onboarding fields beyond name/age need to persist to DB
- Match interests (reach-out + icebreaker flow) still writes to localStorage — needs `match_interests` table
- Outing join/leave still localStorage — needs `outing_members` table
- Icebreakers not generated for DB users (see TODO)
- Viewed/saved matches need DB persistence
- Notifications feed not started
- Reports/blocking not wired to DB
